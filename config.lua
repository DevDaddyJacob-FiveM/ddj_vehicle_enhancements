logger = Logger.new("vehicle-enhancements")

Config = {
    --[[
        Configuration fields for the brake lights module
    ]]
    BreakLights = {
        --[[
            Controls if the module is enabled or not
        ]]
        Enabled = true,
    },
}